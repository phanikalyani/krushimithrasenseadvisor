## YAML Template.
  backend:
    ...
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/krushi
      - SPRING_DATASOURCE_USERNAME=krushi
      - SPRING_DATASOURCE_PASSWORD=krushi
      - ML_SERVICE_URL=http://ml:5001/api/answer
      - JWT_SECRET=replace-with-a-very-long-secret-of-your-choice-CHANGE
      FROM maven:3.9.4-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
COPY src ./src
RUN mvn -DskipTests package

FROM eclipse-temurin:17-jre
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]


---
